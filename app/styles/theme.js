import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      light: '#6ec5ff',
      main: '#2195f2',
      dark: '#0068bf',
      text: '#fff',
    },
    secondary: {
      light: '#ff867c',
      main: '#ef5350',
      dark: '#b61827',
      text: '#fff',
    },
  },
});

export default theme;
